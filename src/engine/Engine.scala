package engine

import asciiPanel.AsciiPanel
import entity.Entity

import scala.collection.mutable

class Engine(terminal: AsciiPanel) {

  val list: mutable.MutableList[Entity] = mutable.MutableList()
//  list += new Entity()
}
